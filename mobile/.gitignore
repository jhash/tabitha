node_modules/

# Native projects are generated by `npx cap add ios` / `npx cap add android`
# (see README.md) — not committed here since they're regenerable from
# capacitor.config.json, and their build artifacts are large/host-specific.
ios/
android/
